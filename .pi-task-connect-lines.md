# 连线功能重写任务

## 目标
修复"只画一半的线"问题,并改成连连看风格(贝塞尔曲线绕行,不穿过标签本体)。

## 需要改 4 个文件

### 1. src/types/ITabManagerState.ts
把 connectLinesData 类型从直线坐标改为 SVG path 字符串:
```
connectLinesData: Array<{ d: string; group: number }>,
```

### 2. src/popup/views/TabManager.tsx — drawConnectLines() 方法
重写为:
- 从每个标签的**右边缘中点**(x = rect.right - containerRect.left, y = rect.top - containerRect.top + rect.height/2)取坐标
- 同组标签按 y 坐标排序后,相邻标签之间画贝塞尔曲线 path
- 控制点:从起点向右偏移 40px,从终点向右偏移 40px,形成向右绕行的弧线
- path 公式: `M x1 y1 C x1+40 y1, x2+40 y2, x2 y2`

### 3. src/popup/views/TabManager.tsx — SVG 渲染段
把 `<line x1=... y1=... x2=... y2=.../>` 改成 `<path d={line.d} fill="none" stroke={color} strokeWidth="2" strokeDasharray="4 2" />`

### 4. css/popup.css — .connect-lines-overlay
加 `overflow: visible !important;` 确保 SVG 不裁剪线条

## 约束
- 不要改其他功能
- 颜色数组保持不变
- connectLinesColors 保持不变
